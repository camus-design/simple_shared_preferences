# example

A example to show simple_shared_preferences.

## Getting Started

1. Add platform what you want to run. For instance, [add-desktop-platforms](https://docs.flutter.dev/desktop#add-desktop-support-to-an-existing-flutter-app)
2. Run `flutter pub get` to install dependencies.
3. Run `flutter run -d macos` to run on macOS.
